{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aorta Abdominal</h1>

    <table>
        <thead>
            <tr>
                <th>Paciente</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for aortaAbdominal in aortaAbdominals %}
            <tr>
                <td><a href="{{ path('aortaabdominal_show', { 'id': aortaAbdominal.id }) }}">{{ aortaAbdominal.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('aortaabdominal_show', { 'id': aortaAbdominal.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a href="{{ path('aortaabdominal_edit', { 'id': aortaAbdominal.id }) }}">Modificar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>

            <a class="btn-floating btn-large waves-effect waves-light green" href="{{ path('aortaabdominal_new') }}"><i class="material-icons">library_add</i></a>Nuevo Estudio
        </li>
    </ul>
{% endblock %}
