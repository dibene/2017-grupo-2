{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aortaabdominals list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for aortaAbdominal in aortaAbdominals %}
            <tr>
                <td><a href="{{ path('aortaabdominal_show', { 'id': aortaAbdominal.id }) }}">{{ aortaAbdominal.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('aortaabdominal_show', { 'id': aortaAbdominal.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('aortaabdominal_edit', { 'id': aortaAbdominal.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('aortaabdominal_new') }}">Create a new aortaAbdominal</a>
        </li>
    </ul>
{% endblock %}
