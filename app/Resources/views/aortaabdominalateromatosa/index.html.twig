{% extends 'base.html.twig' %}

{% block body %}
    <h1>Aorta Abdominal Lateromatosa</h1>

    <table>
        <thead>
            <tr>
                <th>Paciente</th>
                <th>Accciones</th>
            </tr>
        </thead>
        <tbody>
        {% for aortaAbdominalAteromatosa in aortaAbdominalAteromatosas %}
            <tr>
                <td><a href="{{ path('aortaabdominalateromatosa_show', { 'id': aortaAbdominalAteromatosa.id }) }}">{{ aortaAbdominalAteromatosa.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('aortaabdominalateromatosa_show', { 'id': aortaAbdominalAteromatosa.id }) }}">Ver</a>
                        </li>
                        <li>
                            <a href="{{ path('aortaabdominalateromatosa_edit', { 'id': aortaAbdominalAteromatosa.id }) }}">Modificar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            
            <a class="btn-floating btn-large waves-effect waves-light green" href="{{ path('aortaabdominalateromatosa_new') }}"><i class="material-icons">library_add</i></a>Nuevo Estudio
        </li>
    </ul>
{% endblock %}
