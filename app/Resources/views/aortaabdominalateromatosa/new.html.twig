{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Estudio Aorta Abdominal Lateromatosa</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('aortaabdominalateromatosa_index') }}">Volver</a>
        </li>
    </ul>
{% endblock %}
