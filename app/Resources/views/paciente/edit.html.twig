{% extends 'base.html.twig' %}

{% block body %}
<div class="row col s12">
  <br><a class="btn waves-effect waves-light red" style="margin-left:10px; margin-bottom:20px; " href="{{ path('paciente_index') }}">Volver a la lista de pacientes </a>
  <div class="container col s12 center-align" >
    <div class="z-depth-1 grey lighten-4 row left-align col s12" style="display: inline-block; margin: 50px; padding: 32px 48px 50px 48px; border: 1px solid #EEE;">
      <h5 class="red-text center-align ">Editar Paciente</h5>


    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn waves-effect waves-light red" type="submit" value="EDITAR" />
    {{ form_end(edit_form) }}
</div>
</div>
</div>
{% endblock %}
