{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paciente edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value=" Confirmar Edicion" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('paciente_index') }}">Volver al listado de Pacientes</a>
        </li>
        <li>
            {#
            {{ form_start(delete_form) }}
                <input type="submit" value="Borrar">
            {{ form_end(delete_form) }}
            #}
            {{ form_start(delete_form, {'attr': {'onclick': 'return confirm("¿Estas seguro que desea eliminar el Paciente?")'}}) }}
                <input type="submit" value="Borrar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
