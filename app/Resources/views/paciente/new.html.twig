{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Paciente</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('paciente_index') }}">Volver al listado de Pacientes</a>
        </li>
    </ul>
{% endblock %}
