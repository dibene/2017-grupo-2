{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupodiagnostico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupoDiagnostico.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ grupoDiagnostico.descripcion }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ grupoDiagnostico.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('grupodiagnostico_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('grupodiagnostico_edit', { 'id': grupoDiagnostico.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
