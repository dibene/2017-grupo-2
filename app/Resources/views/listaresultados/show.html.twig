{% extends 'base.html.twig' %}

{% block body %}
    <h1>Listaresultado</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ listaResultado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ listaResultado.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ listaResultado.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('listaresultados_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('listaresultados_edit', { 'id': listaResultado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
