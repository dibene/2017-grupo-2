{% extends 'base.html.twig' %}

{% block body %}
<div class="row col s12">
  <div class="container col s12 center-align" >
    <div class="z-depth-1 grey lighten-4 row left-align col s12" style="display: inline-block; margin: 50px; padding: 32px 48px 50px 48px; border: 1px solid #EEE;">

      <h5 class="red-text center-align ">Motivo</h5>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ motivoSolicitud.nombre }}</td>
            </tr>
        </tbody>
            </table>
      </div>  </div>
      </div>
        <ul>
            <li>
              <a class="btn waves-effect waves-light red" style="margin-left:10px; margin-bottom:20px; " href="{{ path('motivosolicitud_index') }}">Volver al listado de Motivos</a>
            </li>
            <li>
              <a class="btn waves-effect waves-light yellow" style="margin-left:10px; margin-bottom:20px; " href="{{ path('motivosolicitud_edit', { 'id': motivoSolicitud.id }) }}">Editar</a>

            </li>
            <li>
                {{ form_start(delete_form) }}
                                <button class="btn waves-effect waves-light red" type="submit" value="Eliminar">
                                  Eliminar
                                  <i class="material-icons right">warning</i>
                                </button>
                {{ form_end(delete_form) }}
            </li>
        </ul>

      {% endblock %}
