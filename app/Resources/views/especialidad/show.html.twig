{% extends 'base.html.twig' %}

{% block body %}
    <h1>Especialidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ especialidad.nombre }}</td>
            </tr>
            <tr>
                <th>Observación</th>
                <td>{{ especialidad.observacion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('especialidad_index') }}">Volver al listado de Especialidades</a>
        </li>
        <li>
            <a href="{{ path('especialidad_edit', { 'id': especialidad.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
